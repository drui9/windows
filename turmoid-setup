#!/usr/bin/bash
if ! [ -d ~/.venv ]; then
    apt update;
    apt install clang python gnupg tree which vim termux-api file;
    python -m venv .venv;
fi

# --
if [ -d ~/.venv ]; then
    source ~/.venv/bin/activate;
    pip install --upgrade pip;
    pip install -r requirements.txt;
fi

